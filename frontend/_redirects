# Netlify redirects file for SPA routing
# This ensures that all routes are handled by React Router

# API redirects (update the URL to your backend deployment)
/api/*  https://your-backend-api.herokuapp.com/api/:splat  200

# SPA fallback - all other routes should serve index.html
/*  /index.html  200
